import { JSX } from "react";

import classes from "./app.module.scss";

const App: React.FC = () => {
  return (
    <div className={classes.wrapper}>
      <h2>Comming Soon</h2>
    </div>
  );
};

export default App;
